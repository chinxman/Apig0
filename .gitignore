# Build artifacts
apig0
apig0.exe
dist/

# macOS metadata
.DS_Store
._.DS_Store
._*
.Spotlight-V100
.Trashes

# R junk (not a Go project)
.Rhistory
.RData

# Go caches
.gocache/
.gomodcache/

# TLS certs
certs/

# Local assistant context, except project-scoped Codex agents
.codex/*
!.codex/agents/
!.codex/agents/*.toml

# Standalone tools (managed separately)
secret-creator/
secret-creator/secret.txt
0.0.2v/secret-creator/secret.txt

# Runtime data files (written by apig0 at startup)
apig0-setup.json
users.json
services.json
ratelimits.json
totp-secrets.json
api-tokens.json
api-token-deliveries.json
access-policies.json
service-secret-metadata.json
service-secrets.json
service-secrets.enc.json
audit.log
*.log
*.pid

# QR code output
*-totp-qr.png

# Security-readiness evidence that should remain tracked
!security/evidence/commands.log
